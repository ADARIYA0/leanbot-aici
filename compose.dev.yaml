services:
  astro:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: leanbot-aici-dev
    ports:
      - "4321:4321"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev -- --host